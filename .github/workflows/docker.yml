name: Docker

on:
  push:
    branches: [master]
    tags:
      - v[0-9]+.[0-9]+.[0-9]+*
  pull_request:
    branches: [master]

jobs:
  build:
    name: Build ${{ matrix.type }} image

    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - type: backend
            context: ./backend
            images: ghcr.io/${{ github.repository_owner }}/secretary-manager-backend
          - type: frontend
            context: ./frontend
            images: ghcr.io/${{ github.repository_owner }}/secretary-manager-frontend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ matrix.images }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha
            type=edge
            type=ref,event=pr  

      - name: Build Docker image (and push on master)
        uses: docker/build-push-action@v6
        with:
          push: true
          context: ${{ matrix.context }}
          file: ${{ matrix.context }}/prod.Dockerfile
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
    permissions:
      contents: read
      packages: write
