import { connect } from "react-redux";
import { submitcode } from "./Code.action-creator.screen";

const mapStateToProps = state => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, null)(ListPuhScreen);
