import { connect } from "react-redux";
import MeetingSettings from "./MeetingSettings.view";

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(MeetingSettings);
