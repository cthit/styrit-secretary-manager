const initialState = {
    tasks: null,
    groups: null,
    group_codes: {}
};

export const MeetingTableReducer = (state = initialState, action) => {
    switch (action.type) {
        case SUBMIT_PASSWORD_SUCCESSFUL:
        default:
            return state;
    }
};
